name: Deploy to Production

on:
  push:
    branches:
      - master
env:
  galaxy_app: td-galaxy
  gatekeeper_app: td-gatekeeper

jobs:
  deploy-router:
    name: Deploy Galaxy Reverse Proxy
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Install Heroku CLI
      run: curl https://cli-assets.heroku.com/install-ubuntu.sh | sh
    - name: Login to Heroku Container registry
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:login
    - name: Push NGINX container
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        GATEKEEPER_URL: https://td-gatekeeper.herokuapp.com
      run: heroku container:push web --app=${{ env.galaxy_app }} --arg GATEKEEPER_URL=https://td-gatekeeper.herokuapp.com
      working-directory: ./router/
    - name: Release NGINX container
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:release web --app=${{ env.galaxy_app }}
      working-directory: ./router/
  deploy-gatekeeper:
    name: Deploy Gatekeeper to heroku
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Checkout Secrets
      uses: actions/checkout@v2
      with:
        repository: tamu-datathon-org/secrets
        token: ${{ secrets.REPO_PAT }}
        path: secrets
    - name: Copy Secrets
      run: |
        cp secrets/gatekeeper.env.production gatekeeper/.env.prod
        ls -la gatekeeper/
    - name: Install Heroku CLI
      run: curl https://cli-assets.heroku.com/install-ubuntu.sh | sh
    - name: Login to Heroku Container registry
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:login
    - name: Push Gatekeeper container
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:push web --app=${{ env.gatekeeper_app }}
      working-directory: ./gatekeeper/
    - name: Release Gatekeeper container
      env: 
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:release web --app=${{ env.gatekeeper_app }}
      working-directory: ./gatekeeper/
